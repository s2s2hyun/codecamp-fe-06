import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: gray;
`;

export default function Layoutfooter() {
  return <Wrapper>여기는 푸터 영역 입니다.</Wrapper>;
}
